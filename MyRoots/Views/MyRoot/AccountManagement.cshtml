
@{
    ViewBag.Title = "AccountManagement";
}

<h2>Zmień swoje dane</h2>



    <section id="user">
    <div>


        <hr />
        <form data-bind="submit: sendChangedData">
            <ul>
                <li style="display:none"></li>
            </ul>
    

    <div class="form-group">
        <label class="col-md-2 control-label" for="OldPassword">Imie</label>
        <div class="col-md-10">
            <input class="form-control" data-val="true" id="OldPassword" name="FirstName" type="text" data-bind="value: applicationUser().firstName, valueUpdate: 'afterkeydown'">
        </div>
        <br />
    </div>
    <div class="form-group">
        <label class="col-md-2 control-label" for="OldPassword">Nazwisko</label>
        <div class="col-md-10">
            <input class="form-control" data-val="true" data-val-required="Last Name is required." id="OldPassword" type="text" data-bind="value: applicationUser().lastName, valueUpdate: 'afterkeydown'">
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Change" class="btn btn-primary" />
        </div>
    </div>
    </form>

        <img data-bind="attr: {src:  applicationUser().image}" />
         <form data-bind="submit: changeImage">
            <input type="file" name="file" accept="image/*" data-bind="event: { change: function() {uploadImage($element.files[0])}}" />
            <input type="submit" value="Zmień" class="btn btn-primary" />
        </form>
       <!-- <form data-bind="submit: setImage">
            <select id="selUserFilterOptions" runat="server" data-bind="value: level, event:{ change: $parent.permissionChanged}">
                <option value="1">Mama</option>
                <option value="2">Tata</option>
                <option value="3">Dziadek</option>
                <option value="4">Babcia</option>
                <option value="5">Syn</option>
                <option value="6">Córka</option>
            </select>
            <input type="submit" value="Zmień" class="btn btn-primary" />
        </form>
           --> </div>

</section>

@section Scripts
{
    <script src="@Url.Content("~/Js/Implementation/AccountManagement.js")"></script>
}
